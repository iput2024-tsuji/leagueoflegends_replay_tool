# 変更履歴

## [0.5.0] - 2026-06-25

### 追加

- 設定画面に録画エンコーダ設定を追加し、「自動（GPU優先）」と「CPU安定（x264）」を選べるようにしました。

### 変更

- OBS録画エンコーダの既定をGPU優先の自動選択に戻し、NVIDIA NVENC、Intel Quick Sync、AMD AMF、x264の順でH.264エンコーダを選択するようにしました。
- 自動選択ではHEVCとAV1を除外し、対応するH.264ハードウェアエンコーダが見つからない場合はx264を使用します。

### 修正

- GPUエンコーダで録画開始に失敗した場合、x264へ切り替えて1回だけ録画開始を再試行するようにしました。
- 「CPU安定（x264）」を選んだ場合に、録画直前の設定適用でGPU優先へ戻ってしまう問題を修正しました。

## [0.4.9] - 2026-06-23

### 修正

- LoL本体のクラッシュやプラクティスモードを通常の試合終了操作なしで閉じた場合でも、録画が終了するようにしました。
- 録画中の終了判定にLCU Gameflow phaseとLoLゲームプロセスの生存確認を追加し、Live Client APIの`GameEnd`が届かないケースを補完しました。
- 録画停止ログに、Live Client、LCU Gameflow、LoLプロセス消失などの終了理由を記録するようにしました。

## [0.4.8] - 2026-06-23

### 修正

- インストール版の初回起動でOBSが作成する `LoL_Replay_Tool` プロファイルを選択し続けないようにし、管理プロファイルを `LoLReplayTool` へ統一しました。
- OBS起動前に `user.ini` のプロファイル選択を `LoLReplayTool` へ固定し、録画開始時点で `Simple / x264 / mkv` の設定が読み込まれるようにしました。
- 録画開始失敗時の診断に、現在のOBSプロファイル、シーンコレクション、出力一覧、`simple_file_output` の状態と保存先を追加しました。

## [0.4.7] - 2026-06-22

### 修正

- OBS録画プロファイルを`Simple / x264 / mkv`へ起動前と実行時の両方で正規化し、古いAdvanced/NVENC設定が残っている環境でも録画開始に失敗しにくくしました。
- OBS録画開始要求のraw応答が失敗を示す場合に明示的にエラー化し、録画失敗時の診断へ出力モード、録画形式、エンコーダ、保存先を含めるようにしました。

## [0.4.6] - 2026-06-21

### 修正

- OBS録画開始時の既定エンコーダをx264に変更し、NVENC初期化失敗による録画開始エラーを避けやすくしました。
- LoLウィンドウキャプチャのプロセス音声キャプチャを無効化し、LoLウィンドウ遷移時の音声キャプチャ失敗が録画開始を妨げにくいようにしました。

## [0.4.5] - 2026-06-21

### 修正

- 録画開始直前のOBS準備で映像設定を再適用しないようにし、OBS本体の出力開始エラーを誘発しにくくしました。
- OBS録画開始前と復旧再試行前に短い安定化待ちを入れ、設定反映直後の録画開始要求による失敗を避けるようにしました。

## [0.4.4] - 2026-06-20

### 修正

- OBS WebSocketの録画開始要求が成功扱いでもOBS本体が録画状態へ移行しない場合に、出力設定を再適用して復旧を試すようにしました。
- 復旧試行ではx264へ切り替え、`ToggleRecord`で1回だけ録画開始を再試行するようにしました。
- 復旧できない場合は、復旧試行の失敗理由とOBSログ診断を録画失敗メッセージへ含めるようにしました。

## [0.4.3] - 2026-06-20

### 修正

- LCUの試合開始検知後、LoL本体のLive Client接続を短時間待ってから録画を開始するようにしました。
- OBSの録画開始確認タイムアウトを延長し、起動直後の初期化遅延で録画開始に失敗しにくくしました。
- 録画開始失敗後に同一試合で連続再試行しないようにし、失敗時のOBSログ診断をエラーへ含めるようにしました。

## [0.4.2] - 2026-06-17

### 変更

- 設定スキーマと実行時設定の共有既定値の参照元を揃え、今後の設定変更が片方だけに反映されるリスクを下げました。
- `AppConfig.paths`からチャンピオン別名ファイルのパスを参照できるようにしました。

### 修正

- 戦術インサイトで同一試合の重複集計を避け、十分なサンプル数がある場合だけ条件を表示するようにしました。
- 戦術インサイトの文言を「観測勝率」に変更し、蓄積データに基づく観測結果であることを明確にしました。
- MPV DLLを後から配置した場合に、同一プロセス内でMPV初期化を再試行できるようにしました。
- OBS ZIP展開時に不正なパスを拒否し、アーカイブ内のパスによる意図しない上書きを防ぐようにしました。
- 録画削除計画の作成中に一時的なI/Oエラーが発生しても、処理全体が止まりにくいようにしました。
- ストレージ上限の自動整理中にファイルのロック、削除、アクセス拒否が起きても、他の整理対象を継続して処理できるようにしました。

## [0.4.1] - 2026-06-13

### 修正

- LCUの専用Gameflow phase APIを試合開始監視へ追加し、Live Client APIやGameflow session APIが利用できない場合でも録画を開始できるようにしました。
- `GameStart`、`InProgress`、`Reconnect`の表記揺れを正規化し、試合開始状態を安定して判定するようにしました。

## [0.4.0] - 2026-06-13

### 追加

- リプレイ画面のBan/Pick順と最終チーム構成に、ローカルで利用可能なチャンピオンアイコンを表示するようにしました。
- チャンピオンアイコンが存在しない場合は、従来どおり文字だけを表示します。アイコンの自動取得は行いません。

### 変更

- ホーム画面の録画状態バッジ下に表示していた内部ログの詳細行を削除しました。
- 録画完了通知を新規設定で既定有効にしました。

### 修正

- 録画完了通知をLoL試合プロセスの終了確認後に送信し、Windowsのゲーム中通知抑制に重なりにくくしました。
- 通知の要求、設定による抑制、トレイへの送信、送信失敗をログへ記録するようにしました。

## [0.3.0] - 2026-06-13

### 追加

- リプレイ画面にBan/Pickタブを追加し、Ban/Pick順と最終チーム構成を確認できるようにしました。
- イベントフィルターをキル、デス、アシスト、ドラゴン、ヘラルド/ヴォイドグラブ、バロン、建造物、その他に分割しました。
- プレイヤーが関与したアシストイベントを録画セッションへ保存するようにしました。

### 変更

- タワーとインヒビターの破壊イベントを建造物イベントとして分類するようにしました。
- イベント分類ロジックを共通化し、録画時の保存判定とリプレイ表示で同じ判定を使用するようにしました。

## [0.2.1] - 2026-06-12

### 追加

- 英語版READMEを追加し、日本語版と相互に切り替えられるようにしました。
- GitHub Release本文を日本語と英語の両方で自動生成するようにしました。

### 変更

- 管理対象OBSの起動ログから利用可能なH.264エンコーダを検出し、NVIDIA NVENC、Intel Quick Sync、AMD AMF、x264の優先順で自動選択するようにしました。
- HEVCとAV1を自動選択対象から除外し、利用可能なハードウェアエンコーダがない場合はx264へフォールバックするようにしました。

## [0.2.0] - 2026-06-11

### 追加

- 録画開始、録画完了、録画失敗、タスクトレイ格納時のWindows通知を追加しました。
- 通知全体のスイッチと、通知イベントごとの個別スイッチを追加しました。
- OBSが録画中になったことを確認した後に録画開始通知を送るようにしました。

### 変更

- ブランチ名、コミットメッセージ、Pull Requestタイトルに作業ツール名を含めず、変更内容を記述するリポジトリルールを追加しました。

## [0.1.4] - 2026-06-11

### 修正

- `gameTime=0`を有効な開始時刻として認識するよう、試合開始検知を改善しました。
- Live Client APIを利用できない場合に、LCU Gameflowの`InProgress`と`Reconnect`を使用するフォールバックを追加しました。
- Live Client APIの状態と現在のLCUフェーズを表示する定期診断ログを追加しました。

## [0.1.3] - 2026-06-10

### 追加

- `144/1`や`240000/1001`などの分数形式による録画フレームレート設定を追加しました。
- OBSのウィンドウキャプチャソースを通じたLoLアプリケーション音声の取得を有効にしました。

### 変更

- OBSのデスクトップ音声を無効化し、手動音声設定をマイク入力のみに変更しました。
